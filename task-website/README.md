# Task Website

This simple website allows a teacher to create tasks for up to two groups of students and collect their responses. It uses Node.js with Express and SQLite.

## Setup

Install dependencies:

```bash
npm install
```

Run the server:

```bash
npm start
```

The site will be available at `http://localhost:3000`.
